@using Microsoft.AspNet.Identity
@if (Context.User.IsInRole("Admin"))
{
    <li>@Html.ActionLink("Teacher", "Index", "Teacher")</li>
}

@if (Context.User.IsInRole("Teacher"))
{
    <li>@Html.ActionLink("Student", "Index", "Student")</li>
    <li>@Html.ActionLink("Manage Projects", "Index", "ProjectTeacher")</li>
}

@if (Context.User.IsInRole("Student"))
{
    <li>@Html.ActionLink("Manage My Projects", "Index", "ProjectStudent")</li>
}
